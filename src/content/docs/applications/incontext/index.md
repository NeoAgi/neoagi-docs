---
title: InContext
description: InContext documentation.
sidebar:
  # Starlight's autogenerated group headers (see the "documentation" section
  # in sites-docs.neoagi.com/astro.config.mjs) are never clickable
  # themselves, so this folder's own landing page still needs its own row in
  # the sidebar - without this override it would show "incontext" twice in
  # a row (the group label and this link).
  label: Overview
---

Empty placeholder - just here to see how the nav shakes out.
